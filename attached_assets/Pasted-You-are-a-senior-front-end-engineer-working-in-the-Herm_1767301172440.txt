You are a senior front-end engineer working in the Hermes SEO / Arclo UI codebase.

Task
Fix the KPI logic on the keyword ranking page so the metrics accurately reflect ranking coverage and remove unnecessary KPIs.

Changes to implement

1. Remove “Average Position” KPI

* Completely remove the **Avg Position / Average Rank** KPI card from this page.
* Do not render it conditionally; it should not exist on this page at all.
* This page is about keyword coverage, not average position.

2. Fix “Keywords Not Ranked” KPI logic

* “Keywords Not Ranked” must be a **derived delta**, not a standalone or API-provided value.
* Definition:

  * `totalKeywords` = total number of target keywords being tracked (e.g., 95)
  * `rankedKeywords` = number of keywords currently ranking (any position > 0)
  * `notRankedKeywords = totalKeywords - rankedKeywords`
* This value should **always** be computed locally in the UI, even if the API provides a value.
* Never allow this KPI to show `0` unless the delta is truly zero.

3. Ensure KPI consistency

* KPIs on this page should now be:

  * Keywords Ranked
  * Keywords Not Ranked (delta)
  * Top 1
  * Top 3
  * Top 10
* All KPI cards should remain visually consistent with existing styling (agent accent color, crown icons unchanged).

Implementation details

* Locate the KPI aggregation logic (likely a selector, hook, or derived state near the keyword table).
* Replace any usage of:

  * `keywordsNotRanked`
  * `notRanking`
  * `unrankedCount`
    coming from the API or worker response.
* Compute `notRankedKeywords` directly from:

  * the same keyword list used to render the table.

Example logic (adjust names to match code):

```ts
const totalKeywords = keywords.length;
const rankedKeywords = keywords.filter(k => Number.isFinite(k.rank) && k.rank > 0).length;
const notRankedKeywords = Math.max(0, totalKeywords - rankedKeywords);
```

* Bind the “Keywords Not Ranked” KPI card to `notRankedKeywords`.

Acceptance criteria

* “Average Position” no longer appears in the KPI row.
* “Keywords Not Ranked” always equals (Total Keywords − Keywords Ranked).
* The KPI never displays an incorrect zero when keywords are unranked.
* Refreshing data or running analysis updates the KPI correctly.
* No regressions to Top 1 / Top 3 / Top 10 counts.

Commit message
`fix(kpis): derive not-ranked keywords from total minus ranked and remove avg position KPI`
