Build a “Crew Manifest” feature in the main Hermes app (the Orchestrator / Services UI). This is a UI + config-driven layer that assigns friendly nicknames, roles, colors, and icons to each backend service (“workers”) while preserving the canonical service_id for all API calls and logic.

GOALS
	1.	Make workers easier to understand at-a-glance (nickname + role + status).
	2.	Keep the system robust: canonical identifiers never change, nicknames are purely presentation.
	3.	Provide a single “Crew Manifest” page and a lightweight “Crew” panel on the existing Integrations/Services page.
	4.	Add a small “Crew feed” of recent activity lines: “Atlas updated 3 articles”, etc.

NON-GOALS
	•	Do not change any worker APIs.
	•	Do not rename service IDs or break existing config/Bitwarden schema.
	•	Do not require backend schema migrations unless necessary; prefer config-only storage.

HIGH-LEVEL UX
A) Integrations / Services Page (existing)
	•	Each service card shows:
	•	Nickname (big)
	•	Role (small)
	•	Status badge (Healthy / Degraded / Down / Disabled)
	•	Last check-in time
	•	Small mono text: service_id
	•	Color dot / border using the crew color
	•	Add a toggle at top-right: “Show Crew Names” (default ON). When OFF, show original service name/service_id as primary label.

B) Crew Manifest Page (new route)
	•	Route: /crew (or /manifest)
	•	Page layout:
	1.	Header: “Crew Manifest”
	•	Subtitle: “Meet the explorers that power Hermes.”
	•	Buttons: “Edit Crew” (if admin), “Export JSON”
	2.	Crew Grid: cards for each service in config
	•	Nickname, Role, Status, service_id
	•	Color badge + icon
	•	“Capabilities” chips (optional)
	•	“Dependencies” section (optional)
	•	“Endpoints” list (optional, read-only)
	3.	Crew Feed: last N activity events (client-side or backend)
	•	Example rows: “🟢 Atlas wrote 3 knowledge articles” (no emoji required; use icon + color instead)

DATA MODEL (CONFIG-DRIVEN)
Create a new config file in the Hermes repo:
	•	File path: src/config/crewManifest.ts (or .json)
	•	Export a typed object keyed by canonical service_id.

Example structure (TypeScript preferred):
export type CrewMember = {
service_id: string;
nickname: string;        // “Atlas”
role: string;            // “Knowledge Cartographer”
color: string;           // hex like “#22C55E”
icon: string;            // lucide icon name, e.g. “Map”
blurb?: string;          // one sentence
capabilities?: string[]; // chips
dependencies?: string[]; // other service_ids
endpoints?: { method: string; path: string; auth: “none”|“api_key” }[];
};

export const CREW_MANIFEST: Record<string, CrewMember> = {
“seo_kbase”: {
service_id: “seo_kbase”,
nickname: “Atlas”,
role: “Knowledge Cartographer”,
color: “#22C55E”,
icon: “Map”,
blurb: “Turns findings into durable knowledge and makes them searchable.”,
capabilities: [“Read Articles”, “Write Articles”, “Search”],
dependencies: [“bitwarden”, “hermes_core”],
endpoints: [
{ method: “GET”, path: “/api/health”, auth: “none” },
{ method: “GET”, path: “/api/articles”, auth: “api_key” },
{ method: “POST”, path: “/api/articles”, auth: “api_key” }
]
},
“seo_compintel”: {
service_id: “seo_compintel”,
nickname: “Scout”,
role: “Recon Specialist”,
color: “#F97316”,
icon: “Binoculars”,
blurb: “Maps competitors, SERPs, and gaps worth attacking.”
}
// Add more here…
};

RULES
	•	If a service_id exists in Hermes service registry but NOT in CREW_MANIFEST, render a default CrewMember:
	•	nickname = TitleCase(service_id)
	•	role = “Specialist”
	•	color = neutral gray
	•	icon = “Bot”
	•	Never use nickname as a key anywhere; always service_id.
	•	Store the “Show Crew Names” toggle in localStorage.

INTEGRATION POINTS
	1.	Identify the existing Hermes “services registry” source of truth (where service list comes from today).
	•	Map each service to a CrewMember by service_id.
	2.	Health/status:
	•	Use existing health checks in Hermes.
	•	Crew UI reads status from the same existing logic.
	3.	Activity feed:
	•	Implement a small local “event bus” in Hermes:
	•	When Hermes calls a worker successfully, log a normalized event:
{ timestamp, service_id, action, summary, status }
	•	Persist in-memory for now (last 100 events). If there is an existing logging store, use it, otherwise keep it simple.
	•	Show these events on /crew and optionally a small widget on Integrations page.

COMPONENTS (React)
Create reusable components:
	•	CrewBadge (color dot + icon)
	•	CrewCard (manifest card)
	•	ServiceCard (existing card updated to include crew fields)
	•	CrewFeed (list)
	•	CrewToggle (Show Crew Names)

TECH STACK ASSUMPTIONS
	•	Hermes is a Node/React app with a UI (likely Vite/React).
	•	Use lucide-react icons if available; otherwise choose from whatever icon library is already installed.
	•	Use existing styling approach (Tailwind or CSS modules). Keep consistent with current app UI.

DELIVERABLES
	1.	New file: src/config/crewManifest.ts
	2.	New page route: /crew with Crew Manifest UI
	3.	Updated Integrations/Services page to show crew nickname + role + color
	4.	LocalStorage toggle: Show Crew Names
	5.	Crew Feed with last 100 events and a simple helper API in the app:
	•	addCrewEvent({ service_id, action, summary, level })
	•	getCrewEvents()

DEFAULT CREW MEMBERS TO ADD NOW
Implement at least these entries:
	•	hermes_core -> “Hermes” / “Mission Control” / deep indigo / icon “Radar”
	•	seo_kbase -> “Atlas” / “Knowledge Cartographer” / green / “Map”
	•	seo_compintel -> “Scout” / “Recon Specialist” / orange / “Binoculars”
	•	seo_tech_audit -> “Wrench” / “Systems Engineer” / slate / “Wrench”
	•	seo_authority -> “Beacon” / “Signal Engineer” / amber / “Radio”
	•	seo_analytics -> “Pulse” / “Telemetry Officer” / cyan / “Activity”
	•	bitwarden -> “Vault” / “Secrets Keeper” / purple / “Key”
	•	(optional) hermes_router -> “Switchboard” / “Dispatcher” / blue / “GitBranch”

ACCEPTANCE CRITERIA
	•	App builds and runs without breaking existing API integrations.
	•	Services page shows crew nickname/role when toggle is ON, and shows original labels when OFF.
	•	/crew route renders all services (including defaults for missing manifest entries).
	•	Colors and icons display consistently.
	•	Crew Feed shows events when Hermes makes calls (at least demonstrate with a couple events).

IMPLEMENTATION NOTES
	•	Do not hardcode service list inside Crew Manifest; manifest is decoration only.
	•	Ensure type safety: unknown service_id gets fallback.
	•	Keep it easy to expand: adding a new worker should be just adding an entry to crewManifest.ts.

Now implement this end-to-end.