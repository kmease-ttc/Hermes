Done. ✅ I made a “make it look like the mock” pack (v3) plus the prompt you can give your agent to apply it.

1) Download the files

Download arclo-dashboard-look-v3.zip￼

What’s inside
	•	src/components/ui/PageHeader.tsx (stronger hierarchy + marketing feel)
	•	src/components/ui/Card.tsx (better separation + gradient top option)
	•	src/components/ui/Button.tsx (consistent radii + gradient primary)
	•	src/components/ui/StatCard.tsx (replaces pastel tiles with crisp stat cards)
	•	src/components/ui/Banner.tsx (replaces the geo warning block cleanly)
	•	src/layout/ContentShell.tsx (adds subtle “marketing wash” background)
	•	src/styles/arclo-contrast-guardrails.css (kills washed-out global opacity/filter issues)
	•	tailwind.tokens.snippet.txt (tokens + card shadows + background wash)
	•	README.txt (install steps)

⸻

2) Prompt for your agent to apply it (copy/paste)

Agent task: Apply Dashboard Look Pack v3
	1.	Add/replace the following files exactly:
	•	src/components/ui/PageHeader.tsx
	•	src/components/ui/Card.tsx
	•	src/components/ui/Button.tsx
	•	src/components/ui/StatCard.tsx
	•	src/components/ui/Banner.tsx
	•	src/layout/ContentShell.tsx
	•	src/styles/arclo-contrast-guardrails.css
	2.	Merge tailwind.tokens.snippet.txt into tailwind.config.ts under theme.extend:
	•	add surface.wash
	•	add shadow.card and shadow.cardHover
	•	ensure backgroundImage.brand-gradient exists
	•	ensure brand.*, surface.*, and text.* tokens exist
	3.	Import the guardrails once in src/index.css (or your global CSS entry):

@import "./styles/arclo-contrast-guardrails.css";


	4.	Refactor the dashboard page (/app/dashboard) to use the new UI components:
	•	Replace the title area with:
	•	<PageHeader title="SEO Performance Overview" highlight="SEO" badgeText="Weekly Report" subtitle="Updated weekly · Rankings are the north star" rightSlot={<SiteSelector/>} />
	•	Replace the “Geographic Scope Required” block with:
	•	<Banner tone="warning" title="Geographic Scope Required" description="Configure your target location to enable SERP analysis and rankings tracking." actionLabel="Configure" onAction={...} />
	•	Replace the four “Cost of Inaction” tiles with StatCard:
	•	label, value, hint, and use accent for each card (purple/orange/pink/etc.)
	•	Replace remaining dashboard containers with Card, using tone="brand" for key sections.
	5.	Optional but recommended:
Wrap dashboard content in <ContentShell> so the content area inherits the subtle marketing wash:
	•	either in AppLayout main content wrapper or in dashboard page itself.

Definition of done:
	•	The dashboard has clear contrast (no washed-out labels)
	•	The dashboard background has a subtle marketing-style wash (optional but recommended)
	•	Cards separate clearly from background
	•	Primary CTAs match marketing gradient style
	•	No global opacity or blur affects the app

⸻

If you want, tell me the exact dashboard file path (it’s probably under src/dashboard/ or src/pages/) and I’ll give you the exact diff-level replacements for the header, banner, and tiles so you can apply it in one pass without hunting.