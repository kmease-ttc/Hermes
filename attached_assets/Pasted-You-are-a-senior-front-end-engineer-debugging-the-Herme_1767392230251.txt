You are a senior front-end engineer debugging the Hermes SEO / Arclo UI.

Context

* Sentinel page (`/agents/content_decay`) is partially rendering: some elements are not loading (blank sections / missing widgets).
* KPI cards are rendering but the accent/highlight styling is wrong: it must use the **Sentinel crew/agent accent color** (like Natasha/Lookout) and follow the standard KPI style guide.

Goal

1. Fix all missing / non-loading UI elements on Sentinel so the page fully renders.
2. Make Sentinel KPI highlights use the **Sentinel crew colors** (agent accent), consistent with the other agents.

Part A — Debug why elements aren’t loading

1. Open browser console and identify errors on `/agents/content_decay`

   * Look for React render errors, undefined access, failed imports, missing assets, failed fetches.
2. Fix the root cause(s), prioritizing:

   * component import paths
   * broken relative asset URLs (e.g., images under `attached_assets` or `generated_images`)
   * API calls returning unexpected shapes (null arrays, missing keys)
   * chart components crashing on empty data
3. Add defensive rendering so the UI never crashes:

   * If data arrays are empty or undefined, render an empty-state card with a CTA (“Run Sentinel scan”)
   * Ensure charts render with 0-values rather than throwing (no NaN / undefined)
4. Verify all sections render:

   * KPI row
   * Missions list
   * Decaying content table
   * Trends charts
   * Any badges/labels in rows

Part B — Fix KPI highlight colors to use Sentinel crew colors

1. Identify the shared KPI components (same used by Natasha/Lookout), e.g. `KeyMetricsGrid` / `KpiCard`.
2. Ensure Sentinel passes the correct agent accent token (e.g. `agentSlug="sentinel"` or `accentColor`).
3. Remove any hardcoded KPI highlight colors in `SentinelContent.tsx`.
4. Update styling so KPI cards use:

   * left/top accent line in Sentinel color
   * hover glow in Sentinel color
   * consistent medal crown colors remain gold/silver/bronze (do not change)
5. Confirm KPI highlight colors match Sentinel’s crew palette in the sidebar (pinky/purple theme for Sentinel, not generic or gray).

Part C — Validate end-to-end

* Reload `/agents/content_decay` with hard refresh
* Confirm:

  * no console errors
  * all widgets render
  * KPI highlights match Sentinel crew color
  * layout matches Natasha/Lookout KPI standard
* Add a lightweight snapshot test or storybook-like render test if available to prevent regression.

Deliverable

* Commit: `fix(sentinel): repair rendering and apply crew accent styling to KPIs`
